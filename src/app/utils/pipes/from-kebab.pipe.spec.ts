import { FromKebabPipe } from './from-kebab.pipe';

describe('FromKebabPipe', () => {
  it('create an instance', () => {
    const pipe = new FromKebabPipe();
    expect(pipe).toBeTruthy();
  });
});
